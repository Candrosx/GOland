pipeline {
    agent any
    stages {
        stage('First step') {
            steps {
                echo '12'
            }
        }
        stage('Second step') {
            steps {
                sh 'ls -l /'
            }
        }        
    }
}
